import React from "react";
import {View, Text, StyleSheet} from 'react-native';

const BoilerplateScreen = () => {
  return <Text>Boilerplate Screen</Text>
};

const styles = StyleSheet.create({});

export default BoilerplateScreen;